import _ from 'underscore';
import Backbone from 'backbone';

const Game = Backbone.Model.extend({
  // This model represents the overall game.
  initialize: function(options) {
  },


});




export default Game;
